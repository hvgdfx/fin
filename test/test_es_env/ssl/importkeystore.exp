
#!/usr/bin/expect -f
spawn /usr/share/elasticsearch/jdk/bin/keytool -importkeystore -srckeystore server.p12 -srcstoretype PKCS12 -destkeystore keystore.jks -deststoretype JKS
expect "Enter destination keystore password:"
send "\r"
expect "Re-enter new password:"
send "\r"
expect "Enter source keystore password:"
send "\r"
expect eof